{
  "scripts": {
    "build": "rsync --archive --verbose --one-file-system  --delete src/ dist/; babel src --out-dir dist",
    "deploy": "rsync --archive --verbose --one-file-system --delete dist/ rcornish@robots.ox.ac.uk:~/WWW/"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  },
  "dependencies": {
    "babel": "^6.23.0"
  }
}
