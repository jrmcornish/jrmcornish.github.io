{
  "scripts": {
    "build": "rsync --archive --verbose --one-file-system  --delete src/ build/; babel src --out-dir build",
    "deploy": "rsync --archive --verbose --one-file-system --delete build/ rcornish@robots.ox.ac.uk:~/WWW/"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
